# Minimal requirements for reproducing RAG baseline (HuggingFace + DPR + FAISS)
transformers>=4.25.0
datasets>=2.4.0
torch>=1.13.0
faiss-cpu==1.7.4
# If using GPU replace with faiss-gpu and use the GPU Dockerfile
# faiss-gpu==1.7.4
accelerate>=0.16.0
sentencepiece
pyserini>=0.12.0
sentence-transformers>=2.2.0
scikit-learn
tqdm
requests
psutil
apache-beam[gcp]

# Optional (for convenience):
# - evaluate for standard metrics
# - wandb for logging
evaluate
wandb

# For BM25/ElasticSearch-based ablations you may need:
# elasticsearch>=7.0.0
# or use Pyserini (above)
